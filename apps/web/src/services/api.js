const baseUrl = import.meta.env.VITE_API_URL || 'http://localhost:4000/api';
async function request(path, options = {}) { const response = await fetch(`${baseUrl}${path}`, { ...options, headers: { 'Content-Type': 'application/json', ...options.headers } }); const data = await response.json(); if (!response.ok) throw new Error(data.message || 'Request failed.'); return data; }
export const api = { getProfile: () => request('/content/profile'), updateProfile: (profile, token) => request('/content/profile', { method: 'PUT', headers: { Authorization: `Bearer ${token}` }, body: JSON.stringify(profile) }), login: (credentials) => request('/auth/login', { method: 'POST', body: JSON.stringify(credentials) }), me: (token) => request('/auth/me', { headers: { Authorization: `Bearer ${token}` } }), signup: (account) => request('/auth/signup', { method: 'POST', body: JSON.stringify(account) }), getForm: (slug) => request(`/forms/${slug}`), submitForm: (slug, values) => request(`/forms/${slug}/submit`, { method: 'POST', body: JSON.stringify(values) }), listForms: (token) => request('/forms', { headers: { Authorization: `Bearer ${token}` } }), createForm: (form, token) => request('/forms', { method: 'POST', headers: { Authorization: `Bearer ${token}` }, body: JSON.stringify(form) }) };
