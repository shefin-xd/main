import bcrypt from 'bcryptjs'; import jwt from 'jsonwebtoken'; import User from '../models/User.js'; import { env } from '../config/env.js'; import { loginSchema, signupSchema } from '../validators/authValidator.js';
const tokenFor = (user) => jwt.sign({ id: user._id, role: user.role, name: user.name }, env.jwtSecret, { expiresIn: '7d' });
const response = (user) => ({ token: tokenFor(user), user: { id: user._id, name: user.name, email: user.email, role: user.role } });
export async function signup(req, res) { const data = signupSchema.parse(req.body); const email = data.email.toLowerCase(); if (await User.exists({ email })) return res.status(409).json({ message: 'An account already exists.' }); const user = await User.create({ ...data, email, password: await bcrypt.hash(data.password, 12) }); res.status(201).json(response(user)); }
export async function login(req, res) { const data = loginSchema.parse(req.body); const user = await User.findOne({ email: data.email.toLowerCase() }).select('+password'); if (!user || !await bcrypt.compare(data.password, user.password)) return res.status(401).json({ message: 'Invalid email or password.' }); res.json(response(user)); }
